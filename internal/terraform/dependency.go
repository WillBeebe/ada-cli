package terraform

func AddDependency(dep string) error {
	// todo: edit providers block, then run terraform init
	// _, err := cmd.StyledExecute(&cmd.CommandOptions{
	// 	Command: fmt.Sprintf("poetry add %s", dep),
	// })

	return nil
}
